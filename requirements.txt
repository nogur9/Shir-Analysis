# Requirements for chart download functionality
# Install these packages to enable chart downloads in the Streamlit app

# Core requirements (from requirements_refactored.txt)
pandas>=1.5.0
numpy>=1.21.0
plotly>=5.0.0
streamlit>=1.25.0
openpyxl>=3.0.0

# Additional requirements for chart downloads
kaleido>=0.2.1  # Required for plotly chart export to PNG
psutil>=5.8.0   # Required by kaleido for process management
